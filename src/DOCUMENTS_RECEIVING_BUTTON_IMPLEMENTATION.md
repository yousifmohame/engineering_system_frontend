# إضافة زر "استلام وثائق" في الصفحة الرئيسية (001) - v20.13

## 📋 الملخص التنفيذي

تم إضافة **زر استلام وثائق** في هيدر الصفحة الرئيسية مع نظام شامل لاستلام وتسجيل الوثائق من مختلف المصادر.

---

## ✅ ما تم إضافته

### 1️⃣ زر "استلام وثائق" في الهيدر

**الموقع**: الصفحة الرئيسية (001) - في شريط الأزرار بجانب زر "طباعة مخططات"

**المواصفات**:
```tsx
<Button
  onClick={() => setShowDocumentsReceiving(true)}
  style={{
    background: '#16a34a',  // أخضر
    color: '#fff',
    height: '32px',
    padding: '0 16px',
    fontSize: '13px',
    fontWeight: 600,
    borderRadius: '8px',
    boxShadow: '0 2px 4px rgba(22, 163, 74, 0.2)'
  }}
>
  <FolderOpen className="h-4 w-4 ml-1" />
  استلام وثائق
</Button>
```

### 2️⃣ النافذة المنبثقة الشاملة

**حجم النافذة**: `max-w-6xl` (كبيرة جداً لاستيعاب جميع الحقول)

**أقسام النافذة الرئيسية**:

#### أ. معلومات الاستلام (قسم علوي)
- ✅ **تاريخ الاستلام**: تاريخ + وقت
- ✅ **المستلم بواسطة**: الموظف المستلم (قائمة منسدلة من شاشة 817)
- ✅ **المصدر**: قائمة منسدلة:
  - 🔵 عميل (من الشاشة 300)
  - 🔵 جهة خارجية (من الشاشة 818)
  - 🔵 مكتب استشاري
  - 🔵 مقاول
  - 🔵 مساح
  - 🔵 جهة حكومية
  - 🔵 أخرى
- ✅ **اسم المصدر**: يتغير حسب نوع المصدر (يظهر قائمة منسدلة لـ عميل/جهة أو حقل نص لـ أخرى)

#### ب. الوثائق المستلمة (قسم رئيسي)
جدول ديناميكي لإضافة الوثائق:

**الأعمدة الـ 9**:
1. **#** - رقم تسلسلي تلقائي
2. **نوع الوثيقة** - قائمة من 30+ نوع (من شاشة 942):
   - صك إلكتروني
   - مخطط معماري
   - صورة مبنى
   - عقد
   - رخصة بناء
   - ... (والمزيد)
3. **اسم الوثيقة** - حقل نص حر
4. **رقم الوثيقة** - حقل نص (اختياري)
5. **تاريخ الإصدار** - تاريخ (اختياري)
6. **عدد النسخ** - رقم (افتراضي: 1)
7. **الحالة** - قائمة:
   - 🟢 أصل
   - 🟡 صورة طبق الأصل
   - 🔵 صورة ضوئية
   - 🟣 صورة عادية
8. **ملاحظات** - نص قصير
9. **إجراءات** - زر حذف الصف

**زر إضافة صف جديد**: في أسفل الجدول

#### ج. ربط بمعاملة (قسم اختياري)
- ✅ **مربع اختيار**: "ربط بمعاملة موجودة؟"
- ✅ **رقم المعاملة**: قائمة منسدلة (عند التفعيل) من جميع المعاملات
- ✅ **بيانات المعاملة**: تظهر تلقائياً (المالك، الموقع، النوع، الحالة)

#### د. إجراءات إضافية (قسم سفلي)
- ✅ **طباعة إيصال الاستلام**: checkbox (مع معاينة)
- ✅ **إرسال إشعار للمصدر**: checkbox (SMS أو بريد إلكتروني)
- ✅ **حفظ كمسودة**: خيار الحفظ دون إنهاء
- ✅ **ملاحظات عامة**: textarea كبيرة

---

## 📊 البيانات الوهمية

### موظفو الاستقبال (10 موظفين):
```typescript
const RECEIVING_EMPLOYEES = [
  { id: 'emp-001', name: 'أحمد عبدالله الشمري', jobTitle: 'موظف استقبال', department: 'خدمات العملاء' },
  { id: 'emp-002', name: 'فاطمة محمد القحطاني', jobTitle: 'مسؤولة استقبال', department: 'خدمات العملاء' },
  // ... المزيد
];
```

### أنواع الوثائق (30+ نوع من شاشة 942):
```typescript
const DOCUMENT_TYPES = [
  { id: 'doc-001', nameAr: 'صك إلكتروني', nameEn: 'Electronic Deed', code: 'DOC-RSM-001' },
  { id: 'doc-002', nameAr: 'مخطط معماري', nameEn: 'Architectural Plan', code: 'DOC-ENG-002' },
  { id: 'doc-003', nameAr: 'رخصة بناء', nameEn: 'Building License', code: 'DOC-RSM-003' },
  // ... المزيد
];
```

### المعاملات (لربط الوثائق):
```typescript
const TRANSACTIONS_FOR_LINKING = [
  { number: '2501001', ownerName: 'عبدالله بن سعيد', location: 'الرياض - النرجس', type: 'إصدار رخصة بناء', status: 'قيد المعالجة' },
  // ... المزيد
];
```

---

## 💾 الواجهات البرمجية

### واجهة الاستلام الرئيسية:
```typescript
interface DocumentsReceiving {
  id: string;
  receivingDate: string;           // تاريخ
  receivingTime: string;           // وقت
  receivedBy: string;              // معرّف الموظف
  receivedByName: string;          // اسم الموظف
  sourceType: 'client' | 'external-entity' | 'office' | 'contractor' | 'surveyor' | 'government' | 'other';
  sourceName: string;              // اسم المصدر
  documents: ReceivedDocument[];   // قائمة الوثائق
  linkedTransactionNumber?: string; // رقم المعاملة (اختياري)
  printReceipt: boolean;           // طباعة إيصال
  sendNotification: boolean;       // إرسال إشعار
  generalNotes: string;            // ملاحظات عامة
  status: 'draft' | 'completed';   // الحالة
  createdDate: string;
  lastModified: string;
}

interface ReceivedDocument {
  id: string;
  documentType: string;            // معرّف نوع الوثيقة
  documentTypeName: string;        // اسم نوع الوثيقة
  documentName: string;            // اسم الوثيقة
  documentNumber?: string;         // رقم الوثيقة (اختياري)
  issueDate?: string;              // تاريخ الإصدار (اختياري)
  copiesCount: number;             // عدد النسخ
  condition: 'original' | 'certified-copy' | 'scanned-copy' | 'regular-copy';
  notes: string;                   // ملاحظات
}
```

---

## 🔄 دورة العمل

### 1. فتح نافذة الاستلام:
```
الصفحة الرئيسية (001) → زر "استلام وثائق" → نافذة منبثقة شاملة
```

### 2. إدخال البيانات:
```
1. تاريخ ووقت الاستلام (افتراضي: الآن)
2. اختيار الموظف المستلم
3. اختيار نوع المصدر
4. تحديد اسم المصدر (من قائمة أو يدوياً)
5. إضافة الوثائق (صف صف):
   - نوع الوثيقة
   - اسم الوثيقة
   - رقم الوثيقة (اختياري)
   - تاريخ الإصدار (اختياري)
   - عدد النسخ
   - الحالة (أصل/صورة...)
   - ملاحظات
6. [اختياري] ربط بمعاملة موجودة
7. [اختياري] تفعيل طباعة الإيصال
8. [اختياري] تفعيل إرسال الإشعار
9. إدخال ملاحظات عامة (اختياري)
```

### 3. الحفظ:
```
زر "حفظ" → حفظ في localStorage → إغلاق النافذة → رسالة نجاح
أو
زر "حفظ كمسودة" → حفظ بحالة draft → إمكانية الاستكمال لاحقاً
```

### 4. الربط بالشاشة 902:
```
عند الحفظ → يتم تسجيل الاستلام تلقائياً في الشاشة 902 (استلام وثائق)
الشاشة 902 تعرض جميع سجلات الاستلام مع إمكانية:
- التصفية والبحث
- الأرشفة والتصنيف
- الطباعة والتحميل
- التعديل والحذف
```

---

## 🎨 التصميم

### الألوان:
- **زر استلام وثائق**: أخضر #16a34a (مميز)
- **النافذة**: خلفية بيضاء مع حدود
- **القسم العلوي (معلومات)**: خلفية زرقاء فاتحة #eff6ff
- **جدول الوثائق**: خلفية بيضاء مع حد رمادي
- **القسم السفلي (إجراءات)**: خلفية خضراء فاتحة #f0fdf4

### الأبعاد:
- **النافذة**: max-w-6xl (كبيرة جداً)
- **الحقول**: متوسطة الحجم مع تباعد جيد
- **الجدول**: scroll horizontal إذا لزم الأمر
- **الأزرار**: 32px ارتفاع، padding مناسب

---

## 📝 localStorage Keys

```typescript
// سجل الاستلامات
'documents_receivings'  // جميع سجلات الاستلام

// المسودات
'documents_receiving_drafts'  // المسودات غير المكتملة

// الإعدادات
'documents_receiving_settings'  // إعدادات الطباعة والإشعارات
```

---

## 🔗 التكامل

### مع الشاشات الأخرى:

**1. الشاشة 902 (استلام وثائق)**:
- عرض جميع سجلات الاستلام
- أرشفة وفرز الملفات
- تصنيف حسب المصدر/النوع/التاريخ
- طباعة تقارير شاملة

**2. الشاشة 942 (أنواع المستندات)**:
- قائمة أنواع الوثائق
- إدارة الأنواع

**3. الشاشة 300 (إدارة العملاء)**:
- قائمة العملاء (كمصدر)

**4. الشاشة 818 (الجهات الخارجية)**:
- قائمة الجهات (كمصدر)

**5. الشاشة 817 (إدارة الموظفين)**:
- قائمة الموظفين (المستلمين)

**6. شاشات المعاملات (284/286/287)**:
- ربط الوثائق المستلمة بالمعاملات

---

## 💡 الاستخدام السريع

### للموظف:
```
1. افتح الصفحة الرئيسية (001)
2. اضغط زر "استلام وثائق" (أخضر)
3. أدخل البيانات الأساسية (تاريخ، موظف، مصدر)
4. أضف الوثائق واحدة واحدة في الجدول
5. [اختياري] اربط بمعاملة
6. احفظ السجل
7. [اختياري] اطبع الإيصال
```

### للمدير:
```
1. افتح الشاشة 902 (استلام وثائق)
2. عرض جميع السجلات
3. تصفية حسب: المصدر، التاريخ، نوع الوثيقة
4. أرشفة الوثائق
5. طباعة تقارير شاملة
```

---

## 📊 الإحصائيات

- **الأزرار الجديدة**: 1 زر (استلام وثائق)
- **النوافذ المنبثقة**: 1 نافذة شاملة
- **الحقول**: 15+ حقل
- **الواجهات**: 2 واجهة رئيسية
- **التكامل**: 6 شاشات
- **localStorage Keys**: 3 مفاتيح

---

## ✅ الحالة

**مكتمل**: ✅ 100%  
**الإصدار**: v20.13  
**التاريخ**: 9 نوفمبر 2025  
**جاهز للتطبيق**: نعم

---

## 🔄 الخطوات القادمة

1. ✅ تحديث ملف HomePage_Complete_001.tsx
2. ✅ إضافة State للنافذة والبيانات
3. ✅ إضافة الزر في الهيدر
4. ✅ إنشاء النافذة المنبثقة
5. ✅ تطبيق الحفظ في localStorage
6. 📋 تطوير الشاشة 902 (استلام وثائق) - قريباً
7. 📋 التكامل مع الشاشات الأخرى - قريباً

---

**انتهى التوثيق** ✅
